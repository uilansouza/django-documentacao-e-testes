version: "2"
services:
  mysql:
    image: mysql:5.7
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 'root'
    volumes:
      - ./database-mysql:/docker-entrypoint-initdb.d/:ro
